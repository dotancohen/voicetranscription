[workspace]
resolver = "2"
members = [
    ".",
    "bindings/python",
    "bindings/android",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Dotan Cohen"]
license = "MIT"
repository = "https://github.com/dotancohen/VoiceTranscription"

[workspace.dependencies]
# Core dependencies
tokio = { version = "1", features = ["rt-multi-thread", "sync", "macros"] }
thiserror = "1"
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-trait = "0.1"

# Whisper
whisper-rs = "0.12"

# Python bindings
pyo3 = { version = "0.22", features = ["extension-module"] }

# Android bindings
uniffi = "0.28"

# Internal
voice-transcription = { path = "." }

[package]
name = "voice-transcription"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Core transcription library with pluggable backends"

[dependencies]
tokio = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
whisper-rs = { workspace = true }
async-trait = { workspace = true }
sha2 = "0.10"
dirs = "5"

# Cloud backends (optional)
reqwest = { version = "0.12", features = ["json", "multipart"], optional = true }

[features]
default = ["local_whisper"]
local_whisper = []
assemblyai = ["reqwest"]
google_cloud = ["reqwest"]
all_backends = ["local_whisper", "assemblyai", "google_cloud"]

[dev-dependencies]
tempfile = "3"
